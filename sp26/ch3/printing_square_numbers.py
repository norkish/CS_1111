# square_numbers.py—Write a program that prints
# A) the numbers 0 to 100 and
# B) their squares.
# Use a for loop with the range(…) function.

for i in range(101):
    print(str(i) + "\t" + str(i*i))
  
